{
  "name": "baize-web",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "build": "pnpm -r --filter './sdk' --filter './ui' run build",
    "test": "pnpm -r run test",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "latest"
  }
}
