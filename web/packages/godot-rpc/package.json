{
  "name": "@baize/godot-rpc",
  "version": "0.1.0",
  "description": "JSON-RPC 契约与传输核心：类型/编解码/配对（RpcClient）/Transport 接口 + ws 实现（零运行时依赖，纯 TS；gd_provider 端到端测试套件）",
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}